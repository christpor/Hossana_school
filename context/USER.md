# USER.md — Operator Profile

## Who You Are
- **Name**: Christ (GitHub: `christpor`).
- **Role**: Cambodian tech lead & AI engineer. Proud Khmer builder; shipped multiple AI-tool workflows and landing pages solo.
- **Time zone**: Cambodia (UTC+7), Phnom Penh.
- **Focus**: Rapid vertical-builds, graduation-capstone momentum, real projects over tutorials.

## Core Values
- **Speed with safety**: Act fast, but never surprise me with irreversible moves.
- **Trust first**: Earned back after a data-loss incident. Ask before deleting, before merging, before any risky git op.
- **Mobile data is precious**: Never download >50MB without warning. Never delete caches/installers — they cost money to re-download.
- **Backup before risk**: `backup-now` (backs up `~/projects` to E: drive) before risky operations.

## Communication Preferences
- **Tone**: Direct, peer-to-peer, zero fluff. Explain in plain English — one idea per sentence. No jargon without a one-line explanation.
- **Workflow**: Always show plan → wait for GO → execute.
- **Never claim victory** without `npm run build` green.
- **Honesty**: If something fails, say so immediately. No hiding failures.

## Machine Constraints
- Lenovo ThinkPad, 2 CPU cores, 8 GB RAM (WSL2 on Ubuntu-24.04, disk on E: drive). No local AI models, no heavy parallel builds — one task at a time.
- Docker Desktop runs on Windows; `postgres-server` container is localhost-only on 5432.
- All projects live in `~/projects` (one folder = one GitHub repo).

## Other AI Tools Installed
`claude`, `codex`, `agy`, `opencode` — interchangeable. All house rules apply regardless of which runs.
